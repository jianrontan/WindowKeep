[package]
name = "windowkeep"
version = "0.1.0"
description = "Local browser workspace snapshots"
authors = []
edition = "2021"

[features]
custom-protocol = ["tauri/custom-protocol"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lints]
workspace = true
